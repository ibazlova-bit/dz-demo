Запускаю таймер на 45 минут и приступаю к составлению тестовых сценариев для формы регистрации.

Цель: максимально покрыть функционал проверками за отведенное время, включая позитивные, негативные и граничные случаи.

План тестирования формы регистрации
Объект тестирования: Форма регистрации с 4 полями:

Имя пользователя (обязательное): кириллица, не пустое, ≤ 255 символов.

Пароль (обязательное): цифры, не пустое, ≤ 64 символов.

Дата рождения (обязательное): строгий формат дд.мм.гггг.

Электронный адрес (необязательное): если заполнено, валидный формат имя@домен.ру, ≤ 255 символов.

Подход: Комбинаторное тестирование и анализ граничных значений.

Чек-лист тестовых сценариев
1. Поле "Имя пользователя" (Обязательное)
Позитивные проверки:

Ввод корректного имени на кириллице (например, "Анна ").

Ввод имени с буквой "ё" (например, "Артём").

Ввод имени с дефисом (например, "Анна-Мария"). (Примечание: в ТЗ про дефис не сказано, но в именах он часто допустим. Это нужно уточнить. Буду проверять как позитивный и негативный случай).

Ввод граничного значения - 255 символов (корректная кириллическая строка длиной 255 символов).

Ввод граничного значения - 1 символ (например, "Я").

Негативные проверки:
6. Пустое поле. Ожидание: сообщение об ошибке.
7. Ввод с латинскими буквами (например, "Anna"). Ожидание: ошибка.
8. Ввод с цифрами (например, "Анна123"). Ожидание: ошибка.
9. Ввод со спецсимволами (например, "Анна!" или "Ан@на"). Ожидание: ошибка.
10. Ввод с пробелами (например, "Анна Юрьевна"). Ожидание: ошибка.
11. Ввод только пробелов. Ожидание: ошибка (трактуется как пустое).
12. Превышение длины - ввод 256 символов. Ожидание: ошибка.
13. Ввод строки, состоящей только из запрещенных символов (например, "123!@#").
14. Попытка SQL-инъекции (например, ' OR '1'='1). Ожидание: ошибка.
15. Попытка XSS-инъекции (например, <script>alert('xss')</script>). Ожидание: ошибка, скрипт не должен выполняться.

2. Поле "Пароль" (Обязательное)
Позитивные проверки:
16. Ввод корректного пароля из цифр (например, "12345").
17. Ввод граничного значения - 64 символа (строка из 64 цифр).
18. Ввод граничного значения - 1 символ (например, "1").

Негативные проверки:
19. Пустое поле. Ожидание: сообщение об ошибке.
20. Ввод с буквами (например, "123pass"). Ожидание: ошибка.
21. Ввод с кириллическими символами (например, "123пароль"). Ожидание: ошибка.
22. Ввод со спецсимволами (например, "123!@#"). Ожидание: ошибка.
23. Ввод с пробелами (например, "12 34"). Ожидание: ошибка.
24. Превышение длины - ввод 65 символов. Ожидание: ошибка.
25. Ввод строки, состоящей только из запрещенных символов (например, "abcde").
26. Проверка на тривиальные пароли (например, "000000", "123456"). (Примечание: хотя в ТЗ нет требования к сложности, это важная проверка безопасности).

3. Поле "Дата рождения" (Обязательное)
Позитивные проверки:
27. Ввод корректной даты, где пользователь совершеннолетний (например, "01.01.2000").
28. Ввод корректной даты, где пользователь сегодня достиг совершеннолетия (например, для сегодняшней даты 15.10.2023 ввести "15.10.2005").
29. Ввод даты с однозначными числами дня и месяца (например, "1.1.2000" -> система должна принять или привести к "01.01.2000"? Уточнить. Пока проверяю как есть).
30. Ввод "граничной" даты - 31 декабря (например, "31.12.1990").
31. Ввод 29 февраля високосного года (например, "29.02.2000").

Негативные проверки:
32. Пустое поле. Ожидание: сообщение об ошибке.
33. Нарушение формата: использование других разделителей (например, "01/01/2000", "01-01-2000").
34. Нарушение формата: отсутствие разделителей (например, "01012000").
35. Ввод несуществующей даты (например, "32.01.2000", "29.02.2001").
36. Ввод некорректного месяца (например, "01.13.2000").
37. Ввод года с двумя цифрами (например, "01.01.00").
38. Ввод даты, где пользователь несовершеннолетний (например, ввод завтрашней даты 15 лет назад: "16.10.2008"). Ожидание: ошибка о возрасте.
39. Ввод даты, где пользователю ровно 17 лет и 364 дня. Ожидание: ошибка о возрасте.
40. Ввод даты из далекого будущего (например, "01.01.2050"). Ожидание: ошибка.
41. Ввод даты из далекого прошлого (например, "01.01.1899"). Ожидание: возможно, ошибка.
42. Ввод текста вместо даты (например, "первое января").
43. Попытка ввода спецсимволов или SQL-инъекции. d

4. Поле "Электронный адрес" (Необязательное)
Позитивные проверки:
44. Отправка формы с пустым полем. Ожидание: успешная регистрация.
45. Ввод корректного email в указанном формате (например, "ivanov@mail.ru").
46. Ввод корректного email с поддоменом (например, "ivanov@domain.subdomain.ru"). (Примечание: в ТЗ указан формат "имя@домен.ру", но домен может быть многоуровневым. Уточнить. Проверяю как позитивный случай).
47. Ввод граничного значения - 255 символов (например, a...255_chars...a@domain.ru).

Негативные проверки:
48. Превышение длины - ввод 256 символов. Ожидание: ошибка.
49. Ввод email без "@" (например, "ivanovmail.ru").
50. Ввод email без домена (например, "ivanov@").
51. Ввод email без имени пользователя (например, "@mail.ru").
52. Ввод email с некорректным доменом верхнего уровня (не ".ру"), например, "ivanov@gmail.com". Ожидание: ошибка.
53. Ввод email с несколькими "@" (например, "iv@anov@mail.ru").
54. Ввод email с запрещенными символами в имени (например, "iv anov@mail.ru", "iv'anov@mail.ru").
55. Ввод email, где имя или домен состоят только из точек или дефисов.

5. Комбинаторные и интеграционные проверки
Позитивные проверки:
56. Идеальный сценарий: Заполнение всех полей (включая email) валидными данными совершеннолетнего пользователя. Ожидание: успешная регистрация.
57. Минимальный сценарий: Заполнение только обязательных полей (Username, Password, Date of Birth) валидными данными. Поле Email пустое. Ожидание: успешная регистрация.

Негативные проверки:
58. Заполнение формы с одним невалидным обязательным полем и валидными остальными. Ожидание: ошибка у соответствующего поля, регистрация не проходит.
* Повторить для каждого обязательного поля (Username, Password, Date of Birth).
59. Заполнение формы с невалидным email (при условии, что он введен). Ожидание: ошибка у поля Email, регистрация не проходит.
60. Проверка поведения кнопки "Зарегистрироваться" при частично заполненной форме.
61. Проверка санитайзации данных: ввод данных с лишними пробелами в начале/конце строки (например, в Username). Ожидание: пробелы должны обрезаться.

6. Проверки, связанные с бизнес-логикой (Возраст)
Расчет возраста: Проверить, что система корректно рассчитывает возраст на момент регистрации, а не на жестко зашитую дату.

Проверка точной даты совершеннолетия: Пользователь родился 15.10.2005. Регистрация 15.10.2023 -> Успех. Регистрация 14.10.2023 -> Ошибка.

Итог
За отведенные 45 минут составлено 63 тестовых сценария.

Этот чек-лист покрывает:

Позитивные и негативные сценарии для каждого поля.

Граничные значения для всех ограничений по длине.

Валидацию форматов (кириллица, цифры, email, дата).

Проверки на безопасность (инъекции).

Комбинаторные проверки взаимодействия полей.

Проверки бизнес-логики (ограничение по возрасту).